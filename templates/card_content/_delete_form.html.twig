<form method="post" action="{{ path('app_card_content_delete', {'id': card_content.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ card_content.id) }}">
    <button class="btn btn-outline-danger mt-2" >Delete</button>
</form>
